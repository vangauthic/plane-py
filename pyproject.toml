[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "plane-py"
version = "0.1.0"
description = "Plane Python Client"
dependencies = []

[tool.setuptools]
packages = ["plane_py"]